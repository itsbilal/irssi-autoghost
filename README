To install this script and run it, please copy it into the ~/.irssi/scripts/
directory ad then load it with the following command in Irssi:

/script load autoghost

You may also symlink to it in the ~/.irssi/scripts/autorun/ directory or copy it there
to get it to run automatically on startup.
